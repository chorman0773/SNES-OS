
namespace std{
    template<typename T,typename traits=char_traits<T>,typename allocator=std::default_allocator<T>> class basic_string{
        int len;
        T* content;
    public:
        
    };

    using string = basic_string<char>;
};
